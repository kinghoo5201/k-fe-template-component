module.exports = async function (initConfig, customConfig) {
  console.log('>>> 初始化完成，正在执行 helper');
  console.log('>>> initConfig', JSON.stringify(initConfig));
  console.log('>>> customConfig', JSON.stringify(customConfig));
}
